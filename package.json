{
  "name": "zen",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ aguxez/zen",
    "create-local": "graph create --node http://localhost:8020/ aguxez/zen",
    "remove-local": "graph remove --node http://localhost:8020/ aguxez/zen",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 aguxez/zen"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.1",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
